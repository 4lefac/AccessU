<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- prevents android "overscroll" effect -->
        <item name="android:overScrollMode">never</item>
    </style>

</resources>
